{
  "name": "@repo/shared",
  "type": "module",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "rimraf dist && tsc -w",
    "build": "rimraf ./src/index.ts && node ./generate-index.js && rimraf dist && tsc -b"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2"
  }
}
